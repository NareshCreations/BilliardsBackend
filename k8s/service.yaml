apiVersion: v1
kind: Service
metadata:
  name: billiards-backend-service
  namespace: billiards-backend
spec:
  selector:
    app: billiards-backend
  ports:
  - protocol: TCP
    port: 80
    targetPort: 3001
  type: ClusterIP
